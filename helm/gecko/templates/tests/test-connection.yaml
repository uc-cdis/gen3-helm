apiVersion: v1
kind: Pod
metadata:
  name: "gecko-test-connection"
  labels:
    {{- include "gecko.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['gecko-service:{{ .Values.service.port }}/health']
  restartPolicy: Never
