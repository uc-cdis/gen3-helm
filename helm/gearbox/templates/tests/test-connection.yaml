apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gearbox.fullname" . }}-test-connection"
  labels:
    {{- include "gearbox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gearbox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
