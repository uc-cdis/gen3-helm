apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "export-secrets-aws.fullname" . }}-test-connection"
  labels:
    {{- include "export-secrets-aws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "export-secrets-aws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
