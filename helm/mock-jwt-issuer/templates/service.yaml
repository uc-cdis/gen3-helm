apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-jwt-issuer.fullname" . }}
  labels:
    {{- include "mock-jwt-issuer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "mock-jwt-issuer.selectorLabels" . | nindent 4 }}
