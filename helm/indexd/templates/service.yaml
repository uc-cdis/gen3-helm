apiVersion: v1
kind: Service
metadata:
  name: indexd-service
  labels:
    {{- include "indexd.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "indexd.selectorLabels" . | nindent 4 }}
