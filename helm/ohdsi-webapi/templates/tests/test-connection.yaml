apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ohdsi-webapi.fullname" . }}-test-connection"
  labels:
    {{- include "ohdsi-webapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ohdsi-webapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
