apiVersion: v1
kind: Service
metadata:
  name: {{ include "ohdsi-webapi.fullname" . }}
  labels:
    {{- include "ohdsi-webapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ohdsi-webapi.selectorLabels" . | nindent 4 }}
