apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gcp-pubsub-indexing.fullname" . }}-test-connection"
  labels:
    {{- include "gcp-pubsub-indexing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gcp-pubsub-indexing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
