apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-search.fullname" . }}-test-connection"
  labels:
    {{- include "ai-search.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-search.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
