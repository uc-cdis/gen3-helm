apiVersion: v1
kind: Service
metadata:
  name: revproxy-service
  labels:
    {{- include "revproxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ ternary 443 .Values.service.port (eq (include "common.nginxSidecar.servicePortName" .) "https") }}
      targetPort: {{ include "common.nginxSidecar.servicePortName" . }}
      protocol: TCP
      name: http
  selector:
    {{- include "revproxy.selectorLabels" . | nindent 4 }}