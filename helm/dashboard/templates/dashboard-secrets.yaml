apiVersion: v1
kind: Secret
metadata:
  name: dashboard-g3auto
type: Opaque
stringData:
  config.json: |-
    {
    "bucket": {{ .Values.dashboardG3auto.bucket | quote }},
    "prefix": {{ .Values.dashboardG3auto.prefix | quote }}
    }
