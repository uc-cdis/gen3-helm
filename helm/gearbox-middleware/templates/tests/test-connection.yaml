apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gearbox-middleware.fullname" . }}-test-connection"
  labels:
    {{- include "gearbox-middleware.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gearbox-middleware.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
