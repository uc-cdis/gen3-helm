{{- $jobsNamespace := default (printf "workflow-pods-%s" .Release.Namespace) .Values.funnel.Kubernetes.JobsNamespace }}

{{- if ne $jobsNamespace .Release.Namespace }}
apiVersion: v1
kind: Namespace
metadata:
    name: {{ $jobsNamespace | quote }}
    labels:
        app.kubernetes.io/name: {{ $jobsNamespace | quote }}
{{- end }}
