apiVersion: v1
kind: ConfigMap
metadata:
  name: karpenter-templates
data:
{{- with .Values.karpenter }}
{{- range $key, $value := . }}
  {{ $key }}: |
    {{- $value | nindent 4 }}
{{- end }}
{{- end}}