apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ohdsi-atlas.fullname" . }}-test-connection"
  labels:
    {{- include "ohdsi-atlas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ohdsi-atlas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
