apiVersion: v1
kind: Service
metadata:
  name: {{ include "presigned-url-fence.fullname" . }}
  labels:
    {{- include "presigned-url-fence.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "presigned-url-fence.selectorLabels" . | nindent 4 }}
