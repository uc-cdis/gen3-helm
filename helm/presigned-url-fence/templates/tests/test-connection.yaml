apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "presigned-url-fence.fullname" . }}-test-connection"
  labels:
    {{- include "presigned-url-fence.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "presigned-url-fence.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
