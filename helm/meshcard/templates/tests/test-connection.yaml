apiVersion: v1
kind: Pod
metadata:
  name: "meshcard-test-connection"
  labels:
    {{- include "meshcard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['meshcard:80/_status']
  restartPolicy: Never
