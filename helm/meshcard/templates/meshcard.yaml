apiVersion: v1
kind: ConfigMap
metadata:
  name: manifest-meshcard
data:
  meshcard.json: |
      {
        "USE_AGG_MDS": "{{ .Values.USE_AGG_MDS }}",
        "AGG_MDS_NAMESPACE": "{{ .Values.AGG_MDS_NAMESPACE }}"
      }